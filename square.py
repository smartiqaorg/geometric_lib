def area(a):
    """Принимает число и выводит квадрат числа"""
    if a >= 0:
        return a * a
    else: 
        return 0

def perimeter(a):
    """Принимает число и выводит число, увеличенное в 4 раза"""
    if a >= 0:
        return 4 * a
    else: 
        return 0
