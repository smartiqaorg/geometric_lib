# Math formulas
## Area
- Circle: S = πR²
- Rectangle: S = ab
- Square: S = a²

## Perimeter
- Circle: P = 2πR
- Rectangle: P = 2a + 2b
- Square: P = 4a

# Документация

В проекте содержатся модули, которые позволяют совершать вычислительные действия по нахождению площади и периметра для определенных геометрических фигур.

---

## Модуль rectangle.py

### Функция area
Получает на вход десятичные два числа `a` и `b`, возвращает десятичное число произведение `a` на `b`.

##### Примеры работы с функцией
```python
[Python]
import rectangle

rectangle.area(2, 3) -> 6

rectangle.area(4, 10) -> 40
```

### Функция perimetr
Получает на вход десятичные числа `a` и `b`, возвращает десятичное число произведение числа 2 на сумму чисел `a` и `b`.

##### Примеры работы с функцией
```python
[Python]
import rectangle

rectangle.perimetr(4, 5) -> 18

rectangle.perimetr(10, 2) -> 24
```

## Модуль circle.py

### Функция area
Получает на вход десятичное число `r`, что явялется радиусом, возвращает произведение числа pi на `r` квадрат.

##### Примеры работы с функцией
```python
[Python]
import circle

circle.area(4) -> 50.24

circle.area(10) -> 314
```

### Функция perimetr
Получает на вход десятичное число r, что явялется радиусом, возвращает произведение числа 2 на r и число pi.

##### Примеры работы с функцией
```python
[Python]
import circle

circle.perimetr(5) -> 31.4

circle.perimetr(10) -> 62.8
```

## Модуль triangle.py

### Функция area
Получает на вход два десятичных числа `a`, `h`, возвращает произведение чисел `a` на `h` и частное от деления на 2.

##### Примеры работы с функцией
```python
[Python]
import triangle

triangle.area(4, 2) -> 4

triangle.area(5, 3) -> 7.5
```

### Функция perimetr
Получает на вход числа `a`, `b`, `c`, возвращает десятичное число сумму чисел `a`,`b`,`c`

##### Примеры работы с функцией
```python
[Python]
import triangle

triangle.perimetr(4, 3, 2) -> 9

triangle.perimetr(3, 1, 9) -> 13
```

## Модуль square.py

### Функция area
Получает на вход дестичное число `a`, возвращает десятичное число `a` квадрат.

##### Примеры работы с функцией
```python
[Python]
import square

square.area(4) -> 16

square.area(11) -> 121
```

### Функция perimetr
Получает на вход десятичное число `a`, возвращает десятичное число произведение числа 4 на число `a`.

##### Примеры работы с функцией
```python
[Python]
import square

square.perimetr(4) -> 16

square.perimetr(5) -> 20
```

## Тесты для функций

| номер теста | предусловия                                                                                                  | последовательность действий                   | полученный результат | ожидаемый результат | вердикт |
|-------------|--------------------------------------------------------------------------------------------------------------|-----------------------------------------------|----------------------|---------------------|---------|
| 1           | Получает на вход десятичное число r, что явялется радиусом, возвращает произведение числа pi на квадрат.     | Вызывает функцию area() от 100                | 314.1592653589793    | 314.1592653589793   | Верно   |
| 2           | Получает на вход десятичное число r, что явялется радиусом, возвращает произведение числа pi на квадрат.     | Вызывает фукнцию area() от 10                 | 31.41592653589793    | 31.41592653589793   | Верно   |
| 3           | Получает на вход десятичное число r, что явялется радиусом, возвращает произведение числа pi на квадрат.     | Вызывает функцию area() от 18                 | 1017.8760197630929   | 1017.8760197630929  | Верно   |
| 4           | Получает на вход десятичное число r, что явялется радиусом, возвращает произведение числа 2 на r и число pi. | Вызывает функцию perimeter() от 15            | 94.24777960769379    | 94.24777960769379   | Верно   |
| 5           | Получает на вход десятичное число r, что явялется радиусом, возвращает произведение числа 2 на r и число pi. | Вызывает функцию perimeter() от 28            | 2463.0086404143976   | 2463.0086404143976  | Верно   |
| 6           | Получает на вход десятичное число r, что явялется радиусом, возвращает произведение числа 2 на r и число pi. | Вызывает функцию perimeter() от -71           | -446.10615680975064  | error               | Неверно |
| 7           | Принимает число a, возвращает квадрат числа a                                                                | Вызывает функцию area() от 7                  | 49                   | 49                  | Верно   |
| 8           | Принимает число a, возвращает квадрат числа a                                                                | Вызывает функцию area() от 17                 | 289                  | 289                 | Верно   |
| 9           | Принимает число a, возвращает квадрат числа a                                                                | Вызывает функцию area() от 104                | 10816                | error               | Неверно |
| 10          | Принимает число a, возвращает произведение числа a на 4                                                      | Вызывает функцию perimeter() от 4             | 16                   | 16                  | Верно   |
| 11          | Принимает число a, возвращает произведение числа a на 4                                                      | Вызывает функцию perimeter() от -17           | -68                  | error               | Неверно |
| 12          | Принимает число a, возвращает произведение числа a на 4                                                      | Вызывает функцию perimeter() от 10            | 40                   | 40                  | Верно   |
| 13          | Принимает числа a, h, возвращает произведение a на h, делёное на 2                                           | Вызывает функцию area() от 12, 14             | 84                   | 84                  | Верно   |
| 14          | Принимает числа a, h, возвращает произведение a на h, деленое на 2                                           | Вызывает функцию area() от -17, 6             | -51                  | error               | Неверно |
| 15          | Принимает числа a, h, возвращает произведение a на h, деленое на 2                                           | Вызывает функцию area() от 10, 20             | 100                  | 100                 | Верно   |
| 16          | Принимает числа a, b, c, возвращает их сумму                                                                 | Вызывает функцию perimeter() от 5, 7, 12      | 24                   | error               | Неверно |
| 17          | Принимает числа a, b, c, возвращает их сумму                                                                 | Вызывает функцию perimeter() от 14.4, 7, 9.1  | 30.5                 | 30.5                | Верно   |
| 18          | Принимает числа a, b, c, возвращает их сумму                                                                 | Вызывает функцию perimeter() от 10, 15, 12    | 37                   | 37                  | Верно   |
| 19          | Принимает числа a, b, возвращает их произведение                                                             | Вызывает функцию area() от 4, 5               | 20                   | 20                  | Верно   |
| 20          | Принимает числа a, b, возвращает их произведение                                                             | Вызывает функцию area() от 17, 4.4            | 74.8                 | 74.8                | Верно   |
| 21          | Принимает числа a, b, возвращает их произведение                                                             | вызывает функцию area() от 28, -41            | -1148                | error               | Неверно |
| 22          | Принимает числа a, b, возвращает их удвоенную сумму                                                          | Вызывает функцию perimeter() от 2, 5          | 20                   | 20                  | Верно   |
| 23          | Принимает числа a, b, возвращает их удвоенную сумму                                                          | Вызывает функцию perimeter() от 16.2, 4.7     | 41.8                 | 41.8                | Верно   |
| 24          | Принимает числа a, b, возвращает их удвоенную сумму                                                          | Вызывает функцию perimeter() от 16, -3        | 26                   | error               | Неверно |

